# 🚀 دليل إعداد Supabase - خطوة بخطوة

## الهدف: ربط المشروع بـ Supabase كقاعدة بيانات سحابية

---

## ✅ الخطوة 1: إنشاء حساب Supabase

### 1.1 التسجيل
1. اذهب إلى: **https://supabase.com**
2. اضغط **"Start your project"** أو **"Sign up"**
3. سجّل بحساب GitHub أو Google

### 1.2 إنشاء مشروع جديد
1. بعد تسجيل الدخول، اضغط **"New Project"**
2. املأ البيانات:
   - **Name**: LuckyWheel
   - **Database Password**: اختر كلمة مرور قوية (احفظها!)
   - **Region**: اختر الأقرب لك
3. اضغط **"Create new project"**
4. انتظر حتى يتم إنشاء المشروع (دقيقة أو دقيقتين)

---

## ✅ الخطوة 2: الحصول على API Keys

### 2.1 افتح Project Settings
1. في Supabase Dashboard، اضغط على أيقونة **⚙️ Settings**
2. اختر **"API"** من القائمة الجانبية

### 2.2 انسخ القيم المطلوبة
ستجد قسمين مهمين:

1. **Project URL**:
   ```
   https://xxxxxxxxxxxxx.supabase.co
   ```

2. **anon public key**:
   ```
   eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
   ```

**انسخ هاتين القيمتين واحفظهما!**

---

## ✅ الخطوة 3: إنشاء الجداول في Supabase

### 3.1 افتح SQL Editor
1. في Supabase Dashboard، اضغط على **"SQL Editor"** من القائمة الجانبية
2. اضغط **"New query"**

### 3.2 انسخ الكود SQL
1. افتح ملف **`supabase-schema.sql`** من مجلد المشروع
2. انسخ كل الكود
3. الصقه في SQL Editor

### 3.3 شغّل الكود
1. اضغط **"Run"** أو **F5**
2. يجب أن ترى رسالة نجاح
3. تحقق من وجود الجداول الثلاثة:
   - ✅ **settings**
   - ✅ **user_data**
   - ✅ **wins**

---

## ✅ الخطوة 4: إعداد متغيرات البيئة (Environment Variables)

### 4.1 إنشاء ملف .env
1. في مجلد المشروع، أنشئ ملف جديد باسم **`.env`**
2. (ملف `.env.example` موجود كمرجع)

### 4.2 أضف القيم
افتح ملف `.env` وأضف:

```env
VITE_SUPABASE_URL=https://xxxxxxxxxxxxx.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
```

**استبدل القيم بالقيم الحقيقية من Supabase!**

### 4.3 تأكد من .gitignore
- ملف `.env` موجود في `.gitignore` (لا يُرفع على GitHub)

---

## ✅ الخطوة 5: تحديث الكود ليستخدم Supabase

الكود تم تحديثه تلقائياً! لكن تأكد من:

1. ملف `src/lib/supabase.js` موجود
2. ملف `.env` يحتوي على القيم الصحيحة
3. أعد تشغيل الخادم بعد إضافة `.env`

---

## ✅ الخطوة 6: اختبار النظام

### 6.1 أعد تشغيل الخادم
```bash
npm run dev
```

### 6.2 افتح التطبيق
1. افتح http://localhost:5173
2. افتح Console (F12)
3. يجب أن ترى رسائل مثل:
   - ✅ "تم الاتصال بـ Supabase بنجاح"

### 6.3 اختبر حفظ البيانات
1. افتح لوحة التحكم (Settings → admin)
2. عدّل أي إعداد
3. احفظ
4. تحقق من Supabase Dashboard → Table Editor → settings

---

## 🔍 التحقق من البيانات في Supabase

### طريقة 1: Table Editor
1. في Supabase Dashboard، اضغط **"Table Editor"**
2. اختر الجدول (settings, user_data, wins)
3. ستجد جميع البيانات

### طريقة 2: SQL Editor
```sql
SELECT * FROM settings;
SELECT * FROM user_data ORDER BY timestamp DESC;
SELECT * FROM wins ORDER BY timestamp DESC;
```

---

## ⚠️ ملاحظات مهمة:

### الأمان:
- ✅ ملف `.env` لا يُرفع على GitHub
- ✅ استخدم `anon key` في الكود (آمن للاستخدام العام)
- ⚠️ لا تشارك `service_role key` أبداً!

### الأداء:
- ✅ Supabase سريع جداً
- ✅ يدعم Real-time subscriptions (اختياري)
- ✅ يدعم Row Level Security (RLS)

### التكلفة:
- ✅ الخطة المجانية كافية للبداية
- ✅ 500 MB storage
- ✅ 2 GB bandwidth/month

---

## 🎯 المميزات:

1. ✅ **أسرع من Google Sheets**
2. ✅ **Real-time updates** (اختياري)
3. ✅ **SQL queries قوية**
4. ✅ **Authentication مدمج** (إذا أردت إضافته لاحقاً)
5. ✅ **Storage للصور والملفات** (إذا أردت)

---

## 📞 حل المشاكل:

### المشكلة: "Invalid API key"
**الحل:**
- تأكد من نسخ `anon key` بشكل صحيح
- تأكد من أن `.env` موجود في جذر المشروع
- أعد تشغيل الخادم

### المشكلة: "relation does not exist"
**الحل:**
- تأكد من تشغيل `supabase-schema.sql` في SQL Editor
- تحقق من وجود الجداول في Table Editor

### المشكلة: البيانات لا تُحفظ
**الحل:**
- افتح Console (F12) وابحث عن الأخطاء
- تأكد من أن RLS غير مفعّل (أو أضف policies)

---

## ✅ تم!

الآن المشروع مربوط بـ Supabase وجاهز للاستخدام! 🎉
