# 🔧 حل مشكلة "رابط Google Script غير محدد"

## المشكلة:
تظهر رسالة: **"⚠️ رابط Google Script غير محدد، استخدام البيانات المحلية"**

## الحل:

### ✅ الخطوة 1: تأكد من أن لديك رابط Google Script

1. افتح **Google Apps Script**
2. اضغط **Deploy** → **Manage deployments**
3. انسخ رابط **Web app URL**
4. الرابط يجب أن يبدو مثل:
   ```
   https://script.google.com/macros/s/AKfycbzIb8HtBlkJRdc6SPQPNTU09pWlt9cd_KsMajmZbu5AQt-4HAu-hw6usEWbDyXIBJj7/exec
   ```

### ✅ الخطوة 2: أدخل الرابط في لوحة التحكم

1. افتح التطبيق في المتصفح
2. اضغط على أيقونة **⚙️ Settings** (أعلى يسار)
3. أدخل كلمة المرور: **admin**
4. ابحث عن قسم **"ربط البيانات (Google Sheet)"**
5. الصق الرابط في حقل **"رابط نشر التطبيق"**
6. تأكد من أن الرابط كامل (يبدأ بـ `https://` وينتهي بـ `/exec`)
7. اضغط **"حفظ التغييرات"**

### ✅ الخطوة 3: تحقق من الحفظ

1. بعد الحفظ، حدّث الصفحة (F5)
2. افتح **Console** (F12)
3. يجب أن ترى رسالة:
   - ✅ **"🔄 جاري تحميل البيانات من: [رابطك]"**
   - وليس: ⚠️ رابط Google Script غير محدد

### ✅ الخطوة 4: إذا لم يعمل

#### تحقق من localStorage:
1. افتح Console (F12)
2. اكتب:
   ```javascript
   localStorage.getItem('googleScriptUrl')
   ```
3. اضغط Enter
4. يجب أن ترى رابطك

#### إذا كان null أو خاطئ:
1. أدخل الرابط مرة أخرى في لوحة التحكم
2. احفظ
3. حدّث الصفحة

---

## 🔍 اختبار سريع:

افتح الرابط في المتصفح مع إضافة `?action=getSettings`:
```
https://script.google.com/macros/s/YOUR_SCRIPT_ID/exec?action=getSettings
```

يجب أن ترى JSON response مثل:
```json
{
  "success": true,
  "settings": {
    "segments": [...],
    ...
  }
}
```

---

## ⚠️ ملاحظات مهمة:

1. **تأكد من نشر التطبيق بشكل صحيح:**
   - Execute as: "أنا (your-email@gmail.com)"
   - Who has access: "أي شخص" (Anyone)

2. **تأكد من نسخ الرابط كاملاً:**
   - يجب أن يبدأ بـ `https://`
   - يجب أن ينتهي بـ `/exec`
   - لا تنسخ مسافات إضافية

3. **بعد إدخال الرابط:**
   - احفظ التغييرات
   - حدّث الصفحة
   - تحقق من Console

---

## ✅ النتيجة المتوقعة:

بعد إدخال الرابط بشكل صحيح:
- ✅ لا تظهر رسالة "رابط Google Script غير محدد"
- ✅ البيانات تُحمّل من السحابة
- ✅ الإعدادات المحفوظة تظهر لجميع المستخدمين
